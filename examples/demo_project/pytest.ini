[pytest]
; Pins this as its own pytest rootdir, independent of the flakemap repo's own
; pyproject.toml, so classnames in the generated JUnit XML look like a real
; standalone project's ("tests.test_suite.*") rather than being prefixed by
; this repo's own directory layout.
